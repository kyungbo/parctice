name = "parctice"
compatibility_date = "2024-09-23"

# Workers Static Assets: dist/ 폴더를 정적 파일로 서빙
# MIME 타입 자동 설정, React Router(SPA) 지원
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
